# Nova — Omni-Agent Workspace (Scaffold)

This repository contains a scaffold for Nova: an omni-agent system split into a React/TypeScript frontend and a Node.js/Express backend with SQLite persistence.

Folders:
- `frontend/` — React + TypeScript (Vite) app skeleton
- `backend/` — Node.js + Express server with SQLite

See each folder README for run instructions.
